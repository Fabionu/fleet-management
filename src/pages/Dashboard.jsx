function Dashboard({ user }) {
  return (
    <div style={{ padding: '40px', textAlign: 'center' }}>
      <h2 style={{ color: 'var(--black)' }}>Dashboard</h2>
      <p style={{ color: 'var(--gray-4)' }}>Coming soon...</p>
    </div>
  );
}

export default Dashboard;